import React from 'react';
import Button from './Button';
import Parent from './Parent';
import Child from './Child';

const App: React.FC = () => {
  return (
    <div>
      <Parent />
    </div>
  );
};

export default App;